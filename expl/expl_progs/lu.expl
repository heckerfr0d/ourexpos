int main()
{
decl
    int temp, i;
    string file, word;
enddecl
begin
    i = 0;
    while (i < 16) do
        temp = exposcall("Getuname", i);
        if (temp != -1) then
            temp = exposcall("Write", -2, temp);
        endif;
        i = i + 1;
    endwhile;
    return 0;
end
}